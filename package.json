{
  "name": "spot-a-dog", "version": "0.1.0", "private": true, "type": "module",
  "description": "Spot relevant words and phrases as you browse.",
  "scripts": {"icons": "node scripts/generate-icons.mjs", "build": "node scripts/build.mjs", "test": "node --test tests/*.test.js", "test:browser": "node tests/browser.mjs", "check": "npm test && npm run build && npm run test:browser"},
  "engines": {"node": ">=20"},
  "devDependencies": {"esbuild": "^0.25.0", "playwright": "^1.55.0"}
}
